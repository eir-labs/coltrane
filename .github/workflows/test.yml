# Cross-environment test matrix. Sweeps typecheck + every deterministic band across
# {ubuntu, macos} ON MAIN and ubuntu-only on pull requests — macOS is billed at
# 10x and none of these jobs is platform-specific (see the `os:` comment below).
# The cross-environment contract is kept; it runs where it pays.
#
# Failure on a single matrix cell isolates the regression to (OS, node-version);
# see CI.md "Interpreting failures" for triage notes.
#
# #262 — what this workflow used to run, and why it changed:
#
#   * `unit` called `npx vitest run`, which is ROOT-CONFIG ONLY. failure_modes, honest_broker,
#     security and e2e were all invisible to it, so this workflow and `npm run verify`
#     disagreed about what "the suite" is. Every job now calls a package.json script, so the
#     thing CI runs is the thing a developer can run.
#   * `e2e-smoke` was a documented stub that echoed a warning and exited 0 — and it was gated
#     to push-to-main, so on a PR it did not even do that. The entire e2e band, 36 files,
#     executed nowhere in automation. It is replaced by `e2e-offline`, which runs the real
#     specs that need no model (tests/e2e/vitest.offline.config.ts). The live half still needs
#     a scripted CLI install + ANTHROPIC_API_KEY and is still out — but it is now the only
#     part that is, and it is one exclude list a reviewer can read.

name: test

on:
  push:
    branches: [main]
  pull_request:

# Cancel superseded runs for the same ref. Saves CI minutes on rapid push.
concurrency:
  group: test-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

# Node 26 only: the floor (26 Sep 2026). --allow-net, the only network gate for a skill, arrived in
# Node 25 (odd, end-of-life June 2026); below it an ungranted skill can fetch freely.
# package.json's engines field says >=26 to match.
jobs:
  typecheck:
    name: typecheck (${{ matrix.os }}, node ${{ matrix.node }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        # macOS runs ON PUSH TO MAIN, not on every pull request. GitHub bills
        # macOS runners at 10x Linux, and none of these three job families is
        # platform-specific: they are typecheck and deterministic test bands
        # over a TypeScript package with no native dependencies, whose only
        # deployment target is Linux (the fly.io drain). Sweeping macOS on
        # every PR bought a signal nobody has acted on, at ten times the price,
        # and it was the largest single multiplier in the account when the
        # minutes ran out (2026-08-27). The cross-environment contract this
        # file exists for is KEPT — it now runs where it pays: on main, before
        # anything ships, instead of on every intermediate commit of every
        # branch.
        os: ${{ github.event_name == 'push' && fromJSON('["ubuntu-latest","macos-latest"]') || fromJSON('["ubuntu-latest"]') }}
        node: ["26.x"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node }}
          cache: "npm"
      - run: npm ci
      - run: npx tsc --noEmit

  unit:
    name: unit (${{ matrix.os }}, node ${{ matrix.node }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        # macOS runs ON PUSH TO MAIN, not on every pull request. GitHub bills
        # macOS runners at 10x Linux, and none of these three job families is
        # platform-specific: they are typecheck and deterministic test bands
        # over a TypeScript package with no native dependencies, whose only
        # deployment target is Linux (the fly.io drain). Sweeping macOS on
        # every PR bought a signal nobody has acted on, at ten times the price,
        # and it was the largest single multiplier in the account when the
        # minutes ran out (2026-08-27). The cross-environment contract this
        # file exists for is KEPT — it now runs where it pays: on main, before
        # anything ships, instead of on every intermediate commit of every
        # branch.
        os: ${{ github.event_name == 'push' && fromJSON('["ubuntu-latest","macos-latest"]') || fromJSON('["ubuntu-latest"]') }}
        node: ["26.x"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node }}
          cache: "npm"
      - run: npm ci
      # `npm test` is now scripts/laws.sh — the EXACT count gate, not a bare `vitest run`.
      # It runs the root band and then asserts every band's file count, so a DELETED law
      # cannot pass by simply reporting a smaller number (verifier n=232: the suite went
      # 3458 -> 3487 in a day with nothing asserting what it should be). The guard lives
      # where the developer runs, and CI calls the same script rather than duplicating it.
      - run: npm test

  bands:
    name: failure-modes + honest-broker + security (${{ matrix.os }}, node ${{ matrix.node }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        # macOS runs ON PUSH TO MAIN, not on every pull request. GitHub bills
        # macOS runners at 10x Linux, and none of these three job families is
        # platform-specific: they are typecheck and deterministic test bands
        # over a TypeScript package with no native dependencies, whose only
        # deployment target is Linux (the fly.io drain). Sweeping macOS on
        # every PR bought a signal nobody has acted on, at ten times the price,
        # and it was the largest single multiplier in the account when the
        # minutes ran out (2026-08-27). The cross-environment contract this
        # file exists for is KEPT — it now runs where it pays: on main, before
        # anything ships, instead of on every intermediate commit of every
        # branch.
        os: ${{ github.event_name == 'push' && fromJSON('["ubuntu-latest","macos-latest"]') || fromJSON('["ubuntu-latest"]') }}
        node: ["26.x"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node }}
          cache: "npm"
      - run: npm ci
      - run: npm run test:failure-modes
      - run: npm run test:honest-broker
      # COLTRANE_LIVE is deliberately NOT set: the prompt-injection scenarios collect and
      # report as skipped, so the band is executed and visible on every PR at zero cost.
      # Setting COLTRANE_LIVE=1 here would spawn the real CLI and bill on every run.
      - run: npm run test:security

  e2e-offline:
    name: e2e (offline half, ubuntu, node 26)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "26.x"
          cache: "npm"
      - run: npm ci
      # Runs the e2e specs that need no model. The script builds first: this band drives
      # dist/ (relay_restart_handshake.spec.ts spawns dist/src/server_entry.js), and a stale
      # dist means testing bytes that are not this branch's.
      #
      # The live half (`npm run e2e`) still needs a scripted claude CLI install +
      # ANTHROPIC_API_KEY and is not wired here — see CI.md "e2e gate" and the exclude list
      # in tests/e2e/vitest.offline.config.ts. That exemption is asserted in writing by
      # tests/test_band_wiring.test.ts so it cannot quietly become the norm again.
      - run: npm run e2e:offline
